using Discord_Bot.Core;
using Discord_Bot.Core.Config;

namespace Discord_Bot.Interactions
{
    public class BiasGameStatInteraction(Logging logger, Config config) : BaseInteraction(logger, config)
    {
        //Todo: Create an interaction for the bias stat buttons, modify the embed accordingly
        //Handle if the given top 10 list is empty (guys most likely)
    }
}
